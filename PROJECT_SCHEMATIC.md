# caffe-game — Схематика проекта (для аналитика)

> Живой документ. Обновляется куратором после каждой итерации улучшений.
> Игра: **«Междумирье»** — нарративный тайкун-симулятор бариста для **Яндекс Игр** (WebGL, десктоп+мобайл). Unity, плагин YG2.
> Жанр-референс: гибрид «time-management кофейня» + «visual novel». Ядро = приготовить кофе по неявному заказу → раскрыть кусочек сюжета.

---

## 1. Сюжет и структура
- Героиня **Мира** (в enum — Anna) держит кофейню «Междумирье» на границе миров; ищет пропавшего мужа **Кая/Андрея**. Антагонист — **Орден Трёх Кругов** и злодейка **Лира**.
- **40 дней** (`Difficulty.FinalDay = 40`), контент на ~42 дня в `StoryDatabase`. Каждый день = 1+ гость.
- Каждый гость — из своего мира (`CharacterType`, ~30 типов), даёт **реплики приветствия → заказ → (если доволен) сюжетное раскрытие / (если нет) реплики недовольства**.
- Между днями: экран результата дня → опц. **виньетка** (спец-эффект + текст) → **«Сон»** (затемнение + эффект из пула + строка кошмара + **ночной эмбиент** «шёпот призраков») → переход. **Порядок звука/рекламы:** музыка глохнет на ночь, во «Сне» играет ночной эмбиент (`AudioController.PlayNight/StopNight`), он выключается на пробуждении → показывается реклама (`Transition`) → и лишь ПОСЛЕ неё возвращается фоновая музыка нового дня.
- **Цель путешествия:** накопить **10000 монет** (`CoinsUI.JourneyGoal`) к дню 40. Не хватило → `JourneyGate`: начать заново (монеты сохраняются) ИЛИ докупить монеты.
- Финал: Кай спасён, светлая концовка.

## 2. Поток одного дня (`DayController.RunDay`)
Заставка «ДЕНЬ N» → по гостям: `ServeCustomer`:
1. Спавн модели гостя (stickman-префаб), ГГ виден за стойкой.
2. Этап 0: гость идёт к стойке (`ProcessVisitor`).
3. Этап 1: приветственный диалог (`DialogueDisplayer.PlayDialogueLines`). Текст появляется **по словам за ~3 с** (`revealDuration`), 1-й клик — показать всю реплику, 2-й клик — следующая (защита `advanceGuard` от быстрого проскока); сбоку мигает «нажмите для продолжения» (`BlinkText`). Бубнёж SpeechMixer.
4. Полоса удовлетворённости = **запомненное** отношение клиента (память по `CharacterType`, старт 50%).
5. **Готовка** (`CoffeeCraftingSystem`): выбор основы → подтвердить → минигейм машины → топпинги → «Подать».
6. Оценка `EvaluateSatisfaction()` (доля из 4 параметров: основа, температура, объём, топпинг).
7. Кружка передаётся гостю; звёзды 1–3⭐ + празднование.
8. Обновление памяти клиента = среднее(старое, новое).
9. Если клиент недоволен (<0.5) → предложить **Комплимент за 50 монет** (+0.3 настроения).
10. **Оплата** (формула ниже) + списание себестоимости.
11. Реакция гостя (эмоция) + сюжетные/недовольные реплики.
12. Гость уходит. Прогресс сохраняется после КАЖДОГО гостя (`SetCustomerIndex`) — обновление страницы продолжает с места.

День провальный ТОЛЬКО если `_coinsEarnedToday < 0` (в минус) → рестарт дня. Провала «за 3 ошибки» нет.

## 3. Экономика (главный рычаг — деньги)
- **Формула оплаты за напиток** (`DayController` стр. 211-217):
  `payment = round( coinsPerCorrectOrder × payScale(=4) × result × (0.5 + tipMood) × EarlyEase(day) × PriceMultiplier(апгрейд) × combo )`
  - `result` 0..1 — качество напитка.
  - `tipMood = clamp01(память клиента + MoodBonus апгрейда)`.
  - `EarlyEase`: ×1.5 день1 → ×1.0 день6 (мягкий старт).
  - `combo`: серия хороших напитков (≥0.6), +0.15/уровень, макс 5 → до ×1.75.
- **Себестоимость:** основа 8 монет, топпинг 3. `_coinsEarnedToday += payment - drinkCost`.
- **Стартовый капитал:** 100 монет.
- **Апгрейды** (`UpgradeShopUI`, макс ур.3): Beans (+12%/ур к оплате, цена 300×ур), Machine (+допуск 0.04/ур, 250×ур), Loyalty (+чаевые 0.06/ур, 200×ур).
- **Сложность** (`Difficulty`): допуск машины 0.22→0.10 к финалу.

## 4. Монетизация (Яндекс)
- **Interstitial (межстраничная):** на каждом переходе между днями (`Transition`).
- **Rewarded (за награду):**
  - «Подсказка» — точный заказ, кнопка **«Уточнить заказ 📺» доступна ВСЕГДА** (добровольно), пульс-подсветка после 2 провалов подряд (`_adHintButton` → `RewardedAdvShow("hint")`). [итер.1]
  - «Нет денег» панель — реклама за монеты (`AdForCoins`, `_noMoneyPanel`).
  - JourneyGate — докупить монеты.
  - **Экран результата дня** (`DayResultUI`): rewarded «Удвоить выручку дня» (id `day_double`, при earned>0) + «Сохранить серию комбо» (id `combo_save`, при серии ≥3 → перенос на завтра) [итер.2].
- **Daily bonus** (`DailyBonusUI`): вход раз в день, растёт со стриком (base + step×(streak-1), капом `maxReward`).
- **Лидерборд** «coins» по общему балансу.
- Звук и геймплей ставятся на паузу на время рекламы и при потере фокуса окна.

## 5. Загадка заказа (ядро вовлечения)
- Игрок видит НЕ точный заказ, а **расплывчатый намёк** (`VagueHint`): «Хочется <основа>, <тёплого>, <в самый раз>…». Точный — только за рекламу-подсказку.
- Соответствие заказа: `type→основа (index%N)`, `sweet→температура` (0.2/0.45/0.65/0.9), `volume→объём` (0.25/0.55/0.85), `topping`.
- **Топпинги (батч рефакторинга):** enum `Topping` = реальные предметы-модели с полки `ShelfItems` (BellPepper, BundtCake, Cookies, Salami, Salmon, Wasabi, Lollipop, Tomato, Pretzel). Билдер (`MakeToppingItems`/`ToppingByName`) сопоставляет топпинг по **имени дочернего объекта**, а не по индексу → визуальная модель = enum = упоминание в реплике совпадают. Реплики гостей переписаны под полочные топпинги (кекс/леденец/печенье; шутка «без васаби»).
- ⚠️ Примечание: `sweet` маппится на ТЕМПЕРАТУРУ машины (не на сахар) — историческое упрощение.

## 6. Минигейм машины (`MachineMinigame`)
Две вертикальные шкалы по очереди (температура, затем объём): бегунок пинг-понгом (`_speed=0.8`), клик фиксирует уровень. Допуск = `Difficulty.Tolerance(day) + апгрейд`. Попал в оба → «В точку!».

## 7. Карта кода (Assets/Scripts)
- **Core/**: `GameManager`(582, singleton, поток игры/дни/сейв/апгрейды/лидерборд/реклама), `DayController`(281, день+экономика), `GameEnums`(типы, `CoffeeOrder`, `CustomerData`, `GameSaveData`), `Difficulty`, `GameInput`(Locked), `Loc`(RU/EN).
- **Gameplay/**: `CoffeeCraftingSystem`(626, готовка/подсказки/комплимент/себестоимость), `MachineMinigame`(97), `CupController`(203), `CustomerController`(352, модель/эмоции/маршрут), `SatisfactionBar`(120), `IngredientItem`, `HeroIdle`.
- **Story/**: `StoryDatabase`(1745, `DayData/DayCustomerEntry/DialogueLine`, контент дней + пул шуток для повторных гостей) + `Assets/StoryDatabase.asset`.
- **Dialog/**: `DialogueDisplayer`(276, печ.машинка/имя/клик/заставки/сообщения), `DialogueManager`(164), `DialogLine`.
- **UI/**: `CoinsUI`, `DayResultUI`, `HintManager`, `UpgradeShopUI`, `DailyBonusUI`, `JourneyGateUI`, `AdForCoins`, `SettingsUI`, `TutorialController`(292), `UiEffects`(263, комбо/звёзды/монеты/баннеры).
- **Audio/**: `AudioController`(звуки: pour/ding/perfect/coin/star/fail…).
- Прочее: `Stages`(машина этапов 0-7 + камера), `ProcessVisitor`(маршрут гостя), `SpeechPlayer/Fragment`(бубнёж), `FlamesController`, FX/SDK/Effects/CutScene.

## 8. Известные ограничения / зацепки для улучшений
- Нет прямой продажи мягкой валюты пакетами (только rewarded за монеты).
- Один гость на день часто → короткая петля; нет «наплыва»/очереди на скорость.
- Нет ежедневных целей/квестов кроме сюжета; нет коллекций/журнала персонажей.
- `sweet→температура` — неинтуитивно, но менять данные дорого.
- Комбо сбрасывается каждый день; нет «мета-прогрессии» украшения кофейни (визуал апгрейдов).
- ~~Реклама-подсказка только после 2 провалов~~ → [итер.1] теперь добровольна всегда. Всё ещё мало ДРУГИХ rewarded «по желанию» (нет удвоения выручки/сохранения комбо на экране результата).

---
## Журнал итераций
_(куратор дополняет после каждого цикла)_

- **Итерация 0 (базлайн):** схематика составлена. Изменений в код ещё не вносилось.
- **Итерация 1 — rewarded-подсказка стала добровольной ✅** (`CoffeeCraftingSystem.cs`, `DayController.cs`). Кнопка «Уточнить заказ 📺» теперь видна на КАЖДОМ заказе, а не только после 2 провалов. Прежний триггер «2 провала» превращён в пульс-подсветку (`SetAdHintHighlight`). Эффект: rewarded-показы доступны все 40 дней добровольно (успешный игрок в потоке тоже может смотреть), без forced-фрустрации. Точный заказ остаётся «страховкой», неявный намёк по-прежнему угадываем без рекламы.
  - _Обновление раздела 4/8 схематики:_ rewarded «подсказка» теперь доступна всегда (не только после провалов) → зацепка «мало rewarded по желанию» частично закрыта.
  - Ещё не сделано из итерации 1: постоянный трекер «монеты/10000» с прогнозом темпа; экран «Отношения»/журнал персонажей.
- **Итерация 2 — «Сохранить комбо» на экране результата ✅** (`DayResultUI.cs`, `DayController.cs`, `GameManager.cs`). Обнаружено: rewarded «Удвоить выручку дня» (`_btnDouble`, id `day_double`) УЖЕ существовал. Добавлена новая rewarded-кнопка «Сохранить серию ×N 📺» (id `combo_save`), доступна при серии ≥3; переносит комбо на следующий день (`GameManager.CarriedCombo`, потребляется в начале дня). Эффект: вторая добровольная rewarded-точка в тёплый момент + мост потока между днями (мотив вернуться завтра). Баланс: перенос одноразовый (сгорает после старта дня), только при достойной серии.
  - Прогноз-трекер «монеты/10000» и наплыв/очередь гостей — предложены аналитиком, пока НЕ сделаны (кандидаты на следующие итерации).
- **Итерация 3 — трекер «Путь к 10 000» с прогнозом темпа ✅** (`DayResultUI.cs`). На экране результата: полоса `total/10000` + цветная строка-прогноз. Логика: needPerDay=(10000−total)/(40−day) vs actualPerDay=total/day → зелёный «Опережаешь»/жёлтый «Впритык»/красный «Отставание ~N/день — удвой выручку 📺». Дни <7 — нейтральное «Разгоняемся…» (искажение EarlyEase). Все UI-поля опциональны (нет ссылки → трекер молчит). Эффект: снимает шок финального гейта, направляет к rewarded «Удвоить» в нужный момент. Зацепка «нет трекера цели» закрыта.
  - Осталось из предложений аналитика: наплыв/очередь гостей (часы пик); журнал персонажей + мета-полки + endless-режим после финала.
- **Итерация 4 — журнал гостей «Завсегдатаи» ✅ (код)** (`SavesYG.Game.cs`, `GameManager.cs`, `DayController.cs`, новые `Core/CharacterNames.cs`, `UI/JournalCard.cs`, `UI/GuestJournalUI.cs`). Делает видимой память отношений: карточка на тип гостя (имя, статус, визиты, лучшие ⭐, симпатия %). Хранение: новые параллельные списки `journalKeys/Visits/BestStars`. Запись визита в `DayController` после подачи. Пассивный бонус «завсегдатая»: симпатия ≥90% → +5% к чаевым. Прогресс «Знакомств X/N» — коллекционная мета, переживающая сюжет.
  - ⚠️ **Требует ручной сборки в Unity-редакторе:** панель журнала + ScrollView(Content) + шаблон-карточка с `JournalCard` (назначить TMP/Image поля) + кнопки «Журнал»/«Закрыть», ссылки в `GuestJournalUI`. Кнопку открытия удобно повесить на экран результата дня. Без арт-ассетов (только TMP/Image/цвета).
  - Осталось: наплыв/«часы пик»; endless-режим после дня 40.
- **Итерация 5 — скилл-бонус «✨ Идеально» в машине ✅** (`CoffeeCraftingSystem.cs`, `DayController.cs`). Попадание точно в центр обеих осей (±0.03, у́же любого допуска) → «✨ Идеально» + ×1.1 к оплате (`PrecisionBonus`). Превращает бинарный клик в градиент мастерства → глубже «поток», честный скилл. Чистая логика, без сборки UI/контента. Баланс: бонус скромный (×1.1) и трудный (±0.03), экономику не инфлирует.
  - **Решение куратора:** рекомендованный аналитиком «Наплыв/очередь гостей» отложен как крупная рискованная инициатива — перестраивает последовательное ядро `ServeCustomer`, требует одновременных моделей/маршрутов и сборки в сцене; внедрять вслепую опасно для рабочей игры. Кандидат на отдельную сессию с запуском Unity. Также в очереди: daily-квесты «Заказ дня», endless после дня 40, реворк семантики осей машины (крепость/температура).
- **Итерация 6 — «Заказ дня» (daily-квест) ✅** (новый `Gameplay/DailyChallenge.cs`, интеграция в `DayController.cs`). Ежедневный детерминированный квест (seed = номер дня → одинаков у всех игроков = топливо для лидерборда). 5 типов: заработать N монет / N× «Идеально» / 3⭐ / продать 2 топпинга / напиток ≥80%. Прогресс копится по дню (`ReportDrink`), награда (60–150+, растёт с днём) начисляется в конце дня и входит в выручку. Анонс через `ShowMessage`, HUD-строка опциональна. Эффект: ежедневная цель (D1/D7 retention), апселл топпингов, повод для лидерборда. Баланс: награда ≤~12% выручки, цели выполнимы при 1 госте/день.
  - ⚠️ Опционально в редакторе: повесить `DailyChallenge` на объект, задать HUD-TMP, привязать ссылку `_dailyChallenge` в `DayController`. Без ссылки — просто не активен (безопасно).
  - Осталось из предложений: апселл «любимый топпинг» (журнал↔экономика); endless-режим после дня 40.
- **Итерация 7 — апселл «любимый топпинг» ✅** (`CharacterNames.cs`, `CoffeeCraftingSystem.cs`, `DayController.cs`). У каждого типа гостя детерминированный любимый топпинг (`CharacterNames.FavoriteTopping`, хэш→1 из 5). При симпатии ≥60% в фазе топпингов — намёк «Гость любит: X ☕». Положил → ×1.08 к оплате и +0.03 симпатии; проигнорировал явную просьбу → −0.02 симпатии. Связывает журнал с экономикой (второй контур ценности лояльности), поднимает средний чек у ядра. Баланс: множитель скромный, штраф мягкий, для новых гостей (симпатия <60%) нейтрально.
  - Осталось: endless-режим после дня 40; онбординг-подсказки новых механик; напоминание о риске стрика.
- **Итерация 8 — онбординг «Первый контакт» ✅** (`SavesYG.Game.cs`, `GameManager.cs`, `DayController.cs`). Одноразовые контекстные подсказки при первом срабатывании механик: `tip_perfect` (Идеально ×1.1), `tip_regular` (завсегдатай/журнал), `tip_favorite` (апселл топпинга), `tip_quest` (Заказ дня синхронен у всех). Флаги в сейве (`shownTips` + `GameManager.MarkTipShown`), показ через `ShowMessage`, максимум 1 подсказка за гостя. Чинит ранний отвал (D1/D3), «зажигает» механики удержания/монетизации у новичка.
  - Осталось: endless-режим после дня 40; «Особый гость»/босс-день (пики вовлечения); напоминание о риске стрика; визуализация апгрейдов.
- **Итерация 9 — дожим экрана результата + предупреждение о стрике ✅** (`DayResultUI.cs`). (1) Loss-aversion: при стрике входа ≥3 строка «Стрик N дней 🔥 загляни завтра — иначе сгорит!» (`_streakWarningText`, опц.). (2) Пульс-дожим кнопки «Удвоить выручку» (мягкая пульсация, `unscaledDeltaTime`, стоп при клике/закрытии). Кнопка уже показывает конкретную сумму `+N`. Эффект: рост CTR rewarded «Удвоить» + D1/D2 retention через напоминание о стрике. Без новой сборки UI (правка существующего экрана).
  - Осталось: endless-режим после дня 40; «Особый гость»/босс-день.
- **Итерация 10 — «Особый гость / Знак дня» ✅** (`DayController.cs`, `CoffeeCraftingSystem.cs`, `GameManager.cs`). Каждый 8-й день (8/16/24/32/40) — особый: ставка ×1.3 (`_specialDayMult`), допуск у́же на −0.02 (`SetExtraTolerance`), анонс в начале дня + тизер «кто-то особенный» накануне в «Сне». Создаёт ритмичные пики вовлечения (якоря-ожидания D8→D40), снижает провал середины. `DayController.IsSpecialDay(day)` — общий хелпер. Баланс: ставку держим ×1.3 (не ×1.4), чтобы стэк с «Удвоить» не ломал темп цели; провал по-прежнему не наказывает сверх обычного.

## ИТОГ 10 ИТЕРАЦИЙ — что внедрено
1. rewarded-подсказка «Уточнить заказ» доступна всегда (добровольно).
2. rewarded «Сохранить комбо» (перенос серии на завтра).
3. Трекер «Путь к 10 000» с цветным прогнозом темпа.
4. Журнал гостей «Завсегдатаи» (видимая память отношений + бонус завсегдатая).
5. Скилл-бонус «✨ Идеально» (±0.03 → ×1.1).
6. «Заказ дня» — ежедневный детерминированный квест (синхронен у всех).
7. Апселл «любимый топпинг» (журнал↔экономика, ×1.08).
8. Онбординг «Первый контакт» (одноразовые контекстные подсказки).
9. Дожим экрана результата (пульс «Удвоить») + предупреждение о сгорании стрика.
10. «Особый гость / Знак дня» (пики вовлечения на днях ×8).

## Батч рефакторинга (полировка UX/атмосферы) — что внедрено
Отдельный проход после 10 итераций LTV (по запросу владельца):
1. **Показ диалога по словам** (`DialogueDisplayer`): вместо посимвольной «печатной машинки» реплика раскрывается по словам за ~3 с (`revealDuration`). 1-й клик — показать всю реплику, 2-й — следующая; `advanceGuard` (~0.35 с) не даёт быстрым кликом проскочить диалог целиком.
2. **Мигающий хинт «нажмите для продолжения»** (`BlinkText`) вместо статичного «далее ▸». Локализован (ru/en).
3. **Вариативность «Заказа дня» до 10 типов** (`DailyChallenge`): + ServeDrinks, StarCollector, HighSatisfaction, TwoStarsPlus, BigEarnings (seed = день, синхронно у всех).
4. **Реструктуризация топпингов** (`GameEnums`, `CoffeeCraftingSystem`, билдер): enum = предметы полки `ShelfItems`, сопоставление по имени объекта; реплики/подсказки приведены в соответствие (см. раздел 5).
5. **Порядок перехода: сначала реклама, потом музыка** (`GameManager`): `ResumeMusic` перенесён на после `Transition` (см. раздел 1).
6. **Обучение (день 1): скрыты все кнопки HUD, кроме настроек** (`TutorialController.HideHudButtons/RestoreHudButtons` — прячет `BtnJournal`, `BtnHint`, возвращает после обучения).
7. **Ночной звук** «шёпот призраков» (`Assets/Audio/night_ambience.mp3` → `AudioController._nightAmbience`, отдельный зацикленный источник): вкл. с началом «Сна», выкл. до рекламы.
8. **Чистка неподдерживаемых символов** в подписях/сообщениях: шрифт Nunito не содержит ✓ ☕ ✨ 🔥 📺 ⭐ ★ ☆ — все удалены из UI-строк кода (звёзды в журнале → «N / 3»), чтобы не было «квадратиков».
9. **Цветные иконки HUD** из `Mini UI/Icons` (Settings/Book/Blue Energy) вместо линейных из `UI Icons`; иконки не тонируются (`Color.white`).
10. **Схематика обновлена** (этот файл + `ARCHITECTURE_MAP.md`).

> ⚠️ После правок кода нажать **Tools → CoffeGame → Build Scene Systems + UI** в Unity: пересоберёт HUD (цветные иконки, мигающий хинт), привяжет `_nightAmbience` и топпинги по именам полки. Импорт `night_ambience.mp3` произойдёт при открытии редактора.

## Главные возможности на будущее (не сделаны)
1. ~~**Endless-режим после дня 40**~~ ✅ Батч 9 + ~~UI рекордов/плашка~~ ✅ Батч 11.
2. ~~**Наплыв/очередь гостей (часы пик)**~~ ✅ Батч 11 — реализовано как скилл-механика темпа поверх последовательной подачи (без перестройки ядра `ServeCustomer`).
3. ~~**Визуализация апгрейдов и мастерства**~~ ✅ Батч 11 — шкала «сейчас/станет» в магазине.

## Батч 11 — закрытие пожеланий пункта 5 (endless-UI + час пик + визуализация апгрейдов)
1. **Endless-UI.** (а) На экране результата в бесконечном режиме — плашка «БЕСКОНЕЧНЫЙ ДЕНЬ N» вместо «День 41…» (`DayResultUI.Show` получил `titleOverride` + `endlessBestDay`; трекер «Путь к 10 000» переиспользован под строку «Рекорд: бесконечный день N»). (б) В настройках — вторая кнопка **«Рекорды: бесконечный режим»** + панель `EndlessLeaderboardPanel` (`LeaderboardYG`, таблица `endless`). Кнопка видна только когда endless открыт (`SettingsUI` скрывает через `GameManager.EndlessActive`).
2. **«Час пик» (`RushController` + `RushHudUI`).** Скилл-механика темпа БЕЗ перестройки ядра: в час-пиковый день (`IsRushDay` — всегда в endless, в сюжете с 6-го дня, `day%3==0`) у каждого гостя мягкий таймер `RushSeconds=22с`; успел подать быстро → множитель оплаты до ×1.30 за темп; не успел → без штрафа (просто нет бонуса). HUD `RushHudUI` (строится в коде, как `AdRemovalPrompt`) — плашка «Час пик · очередь: N» + убывающая шкала. Похвала «Быстро! +X%» через `UiEffects.FloatingText`. Замер по МАСШТАБИРУЕМОМУ времени → пауза настроек честно замораживает. Одноразовая подсказка `tip_rush`.
3. **Визуализация апгрейдов.** В каждом ряду магазина — шкала «сейчас/станет»: полупрозрачный слой = эффект после покупки, поверх — текущий (разница = прибавка). `UpgradeShopUI._effectFills/_effectGhosts`, плавная анимация при покупке. Наглядно показывает «ширину зоны» кофемашины и рост оплаты/чаевых.

> ⚠️ Требуется в Unity: **Tools → CoffeGame → Build Scene Systems + UI** (появятся: кнопка+панель рекордов endless, шкалы «сейчас/станет» в магазине, HUD часа пик). Для таблицы рекордов завести лидерборд **`endless`** в консоли Яндекс Игр (уже требовалось Батчем 9). Часть «час пик»/плашка результата — чистая логика/код, но HUD и шкалы магазина требуют пересборки билдером.

## Сборка сцены — через кнопку-билдер (автоматизировано)
Вся сцена (системы + Canvas + UI + привязка `[SerializeField]`) собирается редакторским скриптом
`Assets/Editor/CoffeGameSceneSetup.cs` → меню **Tools → CoffeGame → Build Scene Systems + UI**
(идемпотентно: удаляет прошлые `--- GAME SYSTEMS ---` и `GameCanvas` и строит заново).

**Батч 6 полностью встроен в билдер** — по нажатию кнопки создаются и привязываются:
- `DailyChallenge` (объект в GAME SYSTEMS) + HUD-строка «Заказ дня» (слева под кассой) → `DayController._dailyChallenge`.
- Экран результата: трекер «Путь к 10 000» (прогресс + полоса + прогноз), предупреждение о стрике, кнопка «Сохранить комбо» — все поля `DayResultUI` привязаны.
- Экран журнала «Завсегдатаи»: панель + ScrollRect(Viewport/Content) + шаблон `JournalCard` + кнопки «Журнал»/«Закрыть», привязка `GuestJournalUI`.
- Хелперы билдера: `HorizontalFill`, `MakeScrollView`, `MakeJournalCard`.

Порядок после правок кода: в Unity нажать кнопку билдера → проверить в Play → сохранить сцену (Ctrl+S) → коммит.
Чисто-логические фичи (Идеально, апселл, особый день, онбординг) работают без сборки.

## Батч 7 — исправления и полировка (сцена сна + топпинги + UI)
1. **Диалог** снова печатается ПОСИМВОЛЬНО за `revealDuration` (~3с), не рывками по словам (`DialogueDisplayer.ShowLineRoutine`, кадровое накопление). 1-й клик — вся реплика, 2-й — дальше.
2. **Комбо-надпись** авто-гаснет через ~2с после показа (`UiEffects.ComboPop`), а не висит до сброса серии.
3. **Топпинги переписаны** (главный баг):
   - Единый источник привязки имя-модели→enum: `ToppingUtil.FromObjectName` (в `GameEnums.cs`); используется и билдером, и `IngredientItem.Awake` в РАНТАЙМЕ — кликнутая модель всегда даёт свой топпинг (салями = Salami, не рыба, не случайно).
   - Один слот: клик выбирает/заменяет, повторный клик снимает; списание денег и падение в кружку — только по кнопке **Подтвердить** (`CoffeeCraftingSystem.SelectTopping`/`OnConfirmTopping`).
   - Заказ НАЗЫВАЕТ топпинг явно (`VagueHint`); случайные заказы берут топпинг только из `ToppingUtil.ShelfToppings` (что реально есть на полке); `SetTargetOrder` снимает недоступный топпинг.
4. **Сцена сна (`Sleepy scene`)** — полная смена сцены между днями. `GameManager.RunGameDays`: итоги+магазин в MainScene → `LoadScene("Sleepy scene")` → `SleepSceneController` (ночной звук + титр «СОН» + эффект + текст сна, строит свой UI в коде) → реклама → `LoadScene("MainScene")`. Состояние — в `YG2.saves` (currentDay/`sleepFromDay`); статик-флаги в GameManager не дают повторить обучение/бонус/GameReady при пере-входе. Запасной путь (старый оверлей в MainScene), если сцена не в Build Settings.
5. **UI-полировка**: цель дня крупнее (HUD 20→30); убрана дублирующая верхне-правая кнопка-подсказка (остаётся пульс-иконка); апгрейды показываются в % эффекта (`+36% → +48%`) вместо «Уровень N/5»; кнопка магазина на итогах пульсирует + бейдж числа доступных апгрейдов + автооткрытие; **кнопка «Убрать рекламу»** (`AdDisableButton`, YG2 Payments, товар `no_ads`, флаг `adsDisabled` глушит интерстишелы); **бейдж журнала** (`JournalBadge`) с числом новых гостей + пульс, сброс при открытии; упрощён жаргон (стрик/апселл → человеческие формулировки).

> ⚠️ Требуется в Unity: **Tools → CoffeGame → Build Scene Systems + UI** (пересобрать MainScene: топпинги, крупная цель, бейдж журнала, кнопка «Убрать рекламу», без боковой подсказки) И **Tools → CoffeGame → Build Sleep Scene** (собрать `Sleepy scene`: `SleepController` + ночной звук + шрифт, добавить обе сцены в Build Settings). Для покупки «Убрать рекламу» — завести товар `no_ads` в консоли Яндекс Игр.

## Батч 8 — новые звуки + органичное отключение рекламы
1. **Все звуковые эффекты заменены** на новый средневеково-фэнтезийный набор (8 клипов в `Assets/Audio/sfx/`: rune_seal, crossbow, axe_slash, sword_draw, horn, bell, fanfare, sting). `BuildSoundBank` теперь грузит ТОЛЬКО их (старый пакет «Casual Game Sounds U6» не используется) и **перезаписывает все 15 событий** банка (переиспользуя клипы по смыслу: клик→axe, гость→horn, подача/монета→bell, «Идеально»/звезда/верно→rune_seal, комбо/бонус/день-успех→fanfare, провал→sting, налив/закрытие→crossbow, открытие→sword). Фон-музыка и бубнёж диалога не тронуты.
2. **Отключение рекламы вплетено органично**: постоянная угловая кнопка (`AdDisableButton`) убрана; вместо неё `AdRemovalPrompt` показывает короткую всплывашку ПОСЛЕ рекламы («Отключить рекламу и поддержать автора?» + кнопка покупки, авто-скрытие ~7с). Вызов — в начале каждого дня (после интерстишела перехода) через `AdRemovalPrompt.Instance.ShowAfterAd()`. Подписи («Убрать рекламу», текст напоминания) добавлены в `UiTranslations` на все 20 языков.

> ⚠️ После этого батча снова прогнать **Build Scene Systems + UI** (пересоберёт `SoundBank.asset` на новые звуки и заменит компонент рекламы). Импорт mp3 из `Assets/Audio/sfx/` произойдёт при открытии редактора.

## Батч 9 — Бесконечный режим + звук голосов + фиксы UX
1. **Бесконечный режим после дня 40 (закрыта гл. возможность №1).** Новый `Core/EndlessMode.cs` процедурно собирает `DayData` (2→5 гостей, детерминированно по seed=дню → корректный resume/reload), сложность зафиксирована на максимуме (Difficulty клампится к дню 40), ставка 50, реплики атмосферные без спойлеров. `GameManager.RunEndlessDays` — цикл после финала: результат дня → сцена сна/реклама → следующий день, счёт дней продолжается (День 41, 42…). Рекорд `endlessBestDay` + отправка в новый лидерборд `endless` (`SubmitEndlessLeaderboard`). Финал (`ShowGameComplete`) теперь ЖДЁТ клик, затем анонс и авто-вход в endless. Сейв: `endlessMode/endlessDay/endlessBestDay`. Переживает перезагрузку (currentDay остаётся 41, endlessMode гейтит финал).
2. **Отдельный канал громкости «Голоса».** Бубнёж героев (`SpeechMixer`) отвязан от «Звуков»: новый `voiceVolume` (сейв), `AudioController.VoiceVolume/SetVoiceVolume/PlayVoicePreview`, третий ползунок «Голоса» в настройках (билдер + `SettingsUI._voiceSlider`), перевод «Голоса» в `UiTranslations` (20 языков). Превью «Звуков» теперь играет реальный SFX (клик), «Голоса» — бубнёж. `ApplySavedVolumes`/`SetVolumes` — 3 аргумента.
3. **Фикс двойной кнопки топпингов.** На этапе топпингов «Подать» и «Подтвердить» больше не висят одновременно: выбор топпинга прячет «Подать» и показывает «Подтвердить»; снятие выбора — наоборот (`CoffeeCraftingSystem.SelectTopping`).
4. **Реклама: боковая кнопка окончательно убрана.** Билдер уже создаёт только `AdRemovalPrompt` (всплывашка после каждой межстраничной рекламы = аналог прежней кнопки), в endless-цикле вызов `ShowAfterAd()` добавлен. Старую боковую кнопку в сцене убирает пересборка билдером.
5. **Разгрузка `Update()` (перф, без изменения ощущения игры).** `Stages.Update` целиком под `#if UNITY_EDITOR` (в билде нет per-frame вызова); `IngredientItem` — пульс подсветки на корутине (десяток предметов не гоняет пустой Update); `JournalBadge` — троттлинг проверки ~2×/сек. Чистая логика, пересборка не требуется. Детали — в `ARCHITECTURE_MAP.md` §4.
6. **SFX приглушены** (`AudioController.SfxGain=0.35`): эффекты по задумке заметно тише музыки/голосов. Дефолты громкости: музыка 0.5 / эффекты 0.8×gain / голоса 0.9.

## Батч 10 — вовлечение (оценка/ярлык) + Яндекс Метрика
1. **Оценка игры** — после дня 3, один раз (`reviewAsked`). Промпт `EngagementPrompt` (строится в коде, без билдера), кнопки локализованы «плашками» (`UiTranslations`). Вызывает `YG2.ReviewShow()` под `#if Review_yg`. ⚠️ Требует установки **модуля Review** в плагине — без него промпт не показывается (нет мёртвой кнопки).
2. **Ярлык на рабочий стол** — после дня 1, один раз (`shortcutAsked`). Плагин YG2 API ярлыка НЕ даёт → свой мост `YaShortcut` + `Assets/Plugins/YaShortcut.jslib` на `ysdk.shortcut` (typeof/try-catch → безопасный no-op). Промпт показывается только если `YaShortcut.Available()`. ⚠️ Проверить на реальной сборке Яндекса.
3. **PlayerStats — НЕ подключаем.** Проверено: это отдельный API целочисленных стат (`SetState/GetState`), не обязателен — весь прогресс уже персистится через модуль Storage (`YG2.saves`). Дублировать не нужно.
4. **Яндекс Метрика — воронка оттока.** Новый `Core/Analytics.cs` (единая точка, `YG2.MetricaSend`). События в точках оттока: `session_start`, `tutorial_start/done`, `day_start`/`day_complete`/`day_failed` (param day+mode, в т.ч. endless), `ad_interstitial` (MainScene+сцена сна), `ad_rewarded`(hint — «застрял»), `purchase`(no_ads/coins_pack), `journey_gate`, `endless_start`, `prompt_shown/accepted`. Разница между соседними `day_start` = точный день ухода. Модуль Metrica включён настройкой (Counter ID в инспекторе плагина) — дефайн не нужен.

> ⚠️ Батч 10 — почти весь РАНТАЙМ/логика, сборка сцены билдером НЕ нужна (промпт строится в коде). Нужно: (а) в плагине задать **Metrica Counter ID** + создать «Цели» в Метрике по именам из `Analytics`; (б) для оценки — **установить модуль Review**; (в) для ярлыка — проверить на боевой сборке Яндекса. PlayerStats не трогаем.

> ⚠️ После Батча 9: прогнать **Build Scene Systems + UI** (появится ползунок «Голоса», исчезнет старая боковая кнопка рекламы, привяжется `_voiceSlider`). Чисто-логические части (endless, фикс топпингов, каналы громкости в коде) работают и без пересборки, но ползунок «Голоса» и удаление боковой кнопки требуют билдера. Для лидерборда рекордов завести таблицу `endless` в консоли Яндекс Игр (без неё отправка молча игнорируется).

## Батч 12 — удержание первых 10 минут + лут-экономика (ветка `feature/retention-batch12`)
Опора на свежие тренды F2P 2025–2026: core-loop <60с и «ага» <90с; прогрессивная выдача механик (снижает перегруз, держит новизну); переменное подкрепление (variable-ratio); эффект Зейгарник; мультивалюта (soft+premium+event); достиженческие/социальные награды. Все новые поп-апы **строятся в коде** (как `EngagementPrompt`), сборка сцены билдером НЕ требуется для их работы.

- **H — Интро (first impression).** Возвращающийся игрок больше НЕ переигрывает пролёт камеры (`SmoothCameraWaypointController.Start` проверяет `introSeen` до пролёта → сразу грузит игру). Кнопка «Пропустить» активна сразу; форс-задержка «Продолжить» 2с→0.5с; текст истории сокращён ~вдвое; `Debug.Log` пролёта под `#if UNITY_EDITOR`. Метрика `intro_start/intro_skip/intro_complete`. (`IntroStoryUI`, `CameraWaypointController`, `Editor/IntroSceneSetup`, `Analytics`.)
- **A — Движок разблокировок (`Core/ProgressionManager.cs`).** Новые механики открываются по дням: лут (D2), кристаллы (D3), кастомизация (D4) — с поп-апом «НОВОЕ ОТКРЫТО» (`UI/RewardPopupUI.cs`, переиспользуется лутом). Тизер «Завтра откроется: …» на конце дня (Зейгарник). В endless всё открыто. Метрика `feature_unlock`. Гейтит только НОВЫЕ механики (не ломает существующий поток).
- **B — Тройная экономика.** Монеты (есть) + **кристаллы** (премиум) + **жетоны** + ключи. `GameManager`: `AddGems/SpendGems/AddTokens/SpendTokens/AddKeys/SpendKeys`, обработчик IAP кристаллов (`gems_small/medium/large`, `starter_pack`) третьим подписчиком `onPurchaseSuccess` (консумирует расходники), `BuyGems(id)` для кнопок. `UI/CurrencyHudUI.cs` — самособирающийся HUD (строки по разблокировкам, монеты не дублирует). Поля сейва `gems/tokens/keys`.
- **C — Переменный лут (`Gameplay/LootSystem.cs`).** Шанс-дроп за каждого гостя (монеты/жетоны/ключ/редко кристалл/мистери) с ранним бонусом шанса и pity-защитой; гарантированный «сундук дня» за успешный день. Мелкое — `FloatingText`, крупное — `RewardPopupUI`. Метрика `loot_drop/chest_open`. Врезка в `DayController.ServeCustomer`/`RunDay`.
- **F — Первые 10 минут.** Заставка дня 2с→1.2с; метрика `first_serve_time` (время до первой подачи на дне 1, цель <90с); тизер разблокировки завтра.
- **E — Возврат.** Оффлайн-доход «кофейня работала, пока тебя не было» (ставка растёт с днём, кап 8ч, порог 10мин; `lastSeenUnix` в `SaveGame`; метрика `offline_income`). ~~7-дневный календарь + таймовый оффер~~ ✅ Батч 13.
- **D — Достижения (`Core/Achievements.cs`).** 10 майлстоунов (знакомства/дни/богатство/рекорд endless) → **кристаллы** (главный не-платный источник премиум-валюты). Оценка в конце дня по данным сейва, суммарный поп-ап, метрика `achievement`. ~~Визуальная кастомизация (аватары/темы)~~ ✅ Батч 13.
- **G — Перф/загрузка WebGL (ручное, в Unity).** См. чек-лист ниже.

### ⚠️ Что сделать в Unity (Батч 12)
1. Открыть проект → сгенерируются `.meta` новых скриптов (`ProgressionManager`, `Achievements`, `LootSystem`, `RewardPopupUI`, `CurrencyHudUI`).
2. **Build Intro (SampleScene)** — пересобрать интро (появится кнопка «Пропустить», привяжется `_skipButton`). Открыть SampleScene → `Tools → CoffeGame → Build Intro`.
3. **G (загрузка/перф):** включить сжатие текстур `Mini UI` (ASTC/DXT) + собрать **SpriteAtlas** по используемым иконкам; удалить неиспользуемые темы/варианты `Mini UI` и не-`_Mobile` префабы `Polygonal Particles`; Build Settings — IL2CPP stripping High, Brotli, exceptions=Explicitly Thrown Only; аудио Load Type (SFX=Decompress, музыка/эмбиент=Streaming). Замерить время до интерактива до/после.
4. ~~_Полировка (опц.):_ магазин кристаллов, экран кастомизации, календарь/оффер, иконки HUD~~ ✅ Батч 13.

### ⚠️ Что завести в консоли Яндекс Игр (Батч 12)
- **Товары IAP:** `gems_small`, `gems_medium`, `gems_large`, `starter_pack` (плюс уже существующие `no_ads`, `coins_pack`).
- **Метрика — новые «Цели»:** `feature_unlock`, `loot_drop`, `chest_open`, `achievement`, `offline_income`, `first_serve_time`, `intro_start`, `intro_skip`, `intro_complete`, `purchase_start`.
- Лидерборды `coins`/`endless` — уже требовались.

> Статус кода: A/B/C/F/H — код-комплит; E/D — полностью закрыты Батчем 13; G — ручные шаги в Unity. Ветка `feature/retention-batch12`. ⚠️ Не протестировано в Play — обязательна прогонка в Unity.

## Батч 13 — закрытие хвостов Батч 12 (магазин кристаллов + календарь/оффер + кастомизация + иконки)
Все новые поп-апы строятся В КОДЕ (кроме экрана кастомизации и валютного HUD — им билдер привязывает спрайты). Локализация — через `Loc.T(ru,en)` (динамика), новых статик-подписей в `UiTranslations` не добавлялось (переиспользуется «Закрыть»).
1. **Магазин кристаллов (`UI/GemShopUI.cs`).** Код-билд поп-ап: 4 товара (`starter_pack` выделен + gems_small/medium/large) → `GameManager.BuyGems(id)`. Вход: тап по строке **«Кристаллы»** в валютном HUD (`CurrencyHudUI` — строка стала кнопкой). Затемняющая подложка, звук.
2. **7-дневный календарь + джекпот (`UI/DailyBonusUI.cs`).** Поверх панели бонуса строится полоса из 7 ячеек (текущий день — золотой, 7-й — джекпот-кристаллы). На 7-й день цикла серии (`((streak-1)%7)+1==7`) сверх монет начисляются **`_jackpotGems=25` кристаллов** (не-платный премиум). Билдер: панель бонуса увеличена, элементы перекомпонованы под полосу (y≈0.62–0.77).
3. **Таймовый оффер (`UI/TimedOfferUI.cs`).** Код-билд всплывашка внизу с обратным отсчётом (60с) на **Стартовый набор**; один раз за сессию, на **дне 3** после рекламы (если реклама не отключена). Замещает обычное напоминание «Убрать рекламу» в этот день.
4. **Кастомизация (`UI/CustomizationUI.cs` + `Core/UiTheme.cs` + `UI/ThemedPanel.cs`).** Экран выбора **аватара** (8 иконок `Mini UI/Avatars`) и **темы** (8 вариантов `Dark Theme RoundEdge`). Выбор персистится (`GameManager.AvatarId/ThemeId → SetAvatar/SetTheme`) и применяется: аватар → **бейдж на HUD** (левый верх; тап открывает экран; виден только после разблокировки D4), тема → `UiTheme.SetPanelSprite` → все панели с `ThemedPanel` (билдер вешает его в `ApplyPanelSprite`) перекрашиваются, в т.ч. открываемые позже (через их `OnEnable`). Спрайты грузит билдер (`Spr()`), сетки выбора строятся в коде. `CustomizationUI` — на всегда-активном Canvas (панель скрыта). Применение на старте: `GameManager.StartGameFlow`.
5. **Иконки валютного HUD.** `CurrencyHudUI` получил `_gemIcon/_tokenIcon/_keyIcon` (Blue Gem / Bronze Ticket / Golden Key), билдер создаёт объект `CurrencyHud` и привязывает иконки (было — цветные «пилюли»).

> ⚠️ Требуется в Unity: **Tools → CoffeGame → Build Scene Systems + UI** (появятся: иконки валют, экран оформления + бейдж аватара, `ThemedPanel` на панелях, увеличенная панель бонуса с календарём). Магазин кристаллов/оффер/джекпот — чистый код, но HUD/иконки/кастомизация требуют пересборки. Товары IAP (`gems_*`, `starter_pack`) — как в Батч 12.

## Батч 14 — полировка к релизу (UI-целостность, поток, экономика)
По итогам плейтеста: новый UI Батч 12–13 плохо согласован со старым, поток рвётся, валюты без смысла. Правки:
1. **UI-наложения.** Валютный HUD схлопнут до одной строки **«Кристаллы»** слева-сверху под кассой (был справа — перекрывал журнал/подсказку). Бейдж аватара удалён (перекрывал кассу).
2. **Кастомизация УДАЛЕНА полностью** (`CustomizationUI/UiTheme/ThemedPanel`, бейдж, аватар/тема-спрайты, `Feature.Customization`, `ThemedPanel` в `ApplyPanelSprite`, аксессоры `AvatarId/ThemeId`). Сейв-поля `avatarId/themeId/ownedCustomizations` оставлены мёртвыми (совместимость).
3. **Экономика упрощена до монеты + кристаллы.** Жетоны/ключи убраны из лута/HUD (методы `AddTokens/AddKeys` оставлены мёртвыми). `LootSystem` даёт монеты/кристаллы/мистери. **Сток кристаллов** (`GemShopUI` секция «Потратить»): мгновенный апгрейд кофейни (`GameManager.BuyUpgradeWithGems`, `GemUpgradeCost=40`) и убрать рекламу (`RemoveAdsWithGems`, `GemRemoveAdsCost=250`). Анонс разблокировки кристаллов прямо говорит, куда тратить.
4. **Ядро — двойной тап** вместо кнопки «Подтвердить» на основе: 1-й тап по предмету подсвечивает (пульс-масштаб), 2-й тап по нему же — наливает (`CoffeeCraftingSystem.OnItemClicked`). Обучение обновлено. Топпинги — как были (кнопка подтверждения осталась, топпинг платный).
5. **Гости живее:** `ProcessVisitor.moveSpeed 2→2.6` (билдер), `Animator.speed=1.15` (спавн).
6. **Интро:** пролёт камеры играет ВСЕГДА (убран ранний skip в `SmoothCameraWaypointController`); для вернувшегося пропускается только длинный ТЕКСТ (в `IntroStoryUI`).
7. **Бубнёж синхронизирован** с печатью: ретригер фрагментов, пока печатается, стоп на полном показе (`DialogueDisplayer` + `SpeechMixer.IsPlaying/StopSpeech`).
8. **Стена попапов в начале дня устранена:** `ShowMessage` теперь одиночный (новый отменяет прежний → нет наложения текста); анонсы дня идут по очереди через `DayController.Announce`, после закрытия поп-апа разблокировки (`WaitForUnlockPopup`). Таймовый оффер уведён с дня 3 на **день 5** и не поверх `RewardPopupUI`.
9. **Сон — только отдельная сцена.** Удалён запасной in-MainScene `PlayDreamVignette` (при ложном `SleepSceneAvailable()` включалась «старая» реализация вместе с новой). Теперь запасной путь — просто затемнение+реклама.

> ⚠️ После Батч 14: прогнать **Build Scene Systems + UI** (валютный HUD слева, без кастомизации/бейджа, скорость гостя, без `ThemedPanel`). Логика (двойной тап, бубнёж, секвенс анонсов, сон, сток кристаллов) работает и без пересборки. Консоль Яндекса — как в Батч 12 (IAP `gems_*`/`starter_pack`, Метрика, лидерборды).

## Батч 15 (Фаза A) — вовлечение: обустройство кофейни = смысл монет
Плейтест: «не затягивает», у монет нет цели. Внедрён центр вовлечения — **мета обустройства** (Gardenscapes-подход), даёт монетам понятный сток и постоянную цель.
1. **`Core/RenovationManager.cs` (static)** — лестница из 12 «проектов» (кофемашина→стулья→ковёр→витрина→люстра→полка→знамёна→бариста-стол→вывеска→кристалл→терраса→2-й этаж). Каждый: цена в монетах (400…13000), выгода (`Benefit` Price/Tip/Tolerance — складываются, читаются через `GameManager.PriceMultiplier/MoodBonus/ToleranceBonus`) и сюжетный бит. `Complete()` списывает монеты и двигает стадию; `CompleteWithGems()` (30 крист.) — мгновенно. Сейв `renovationStage`.
2. **`UI/RenovationGoalUI.cs` (singleton, код-билд)** — постоянный **виджет цели** в левой колонке под кассой/кристаллами: «Копим: <проект> A/B» + полоса; тап открывает панель проекта (сюжет+выгода+«Обустроить»/«Ускорить»). Главный «грид денег». `Ensure()` после обучения (`GameManager.StartGameFlow`).
3. **`Gameplay/RenovationVisualizer.cs` (билдер)** — ставит мебель-префабы `LowPolyDungeon` по опорным точкам зала на завершённые стадии (`_stagePrefabs[12]`+`_anchors[12]`), «поп»-анимация; опц. `ShowcaseStage` (камера к зоне, вне подачи). Восстанавливает обустроенное при старте. **Якоря — в ПЕРСИСТЕНТНОМ `RenoAnchors`** (билдер лишь досоздаёт недостающие; позиции сохраняются) — **ПЕРЕСТАВИТЬ якоря по залу в сцене**.
4. **Near-miss (Royal Match):** при `result` близко к 3★/порогу — «Так близко!»/«Чуть-чуть не хватило» (`DayController`), подпитывает «ещё чашку».

5. **Ежедневная доска задач** (`Core/DailyTaskBoard.cs` + `UI/DailyTaskBoardUI.cs`) — заменила одиночный «Заказ дня». 3 детерминир. задачи/день (пул `DailyChallenge.Kind`), кнопка «Задачи дня» в левой колонке + бейдж доступных наград; клейм за каждую → монеты СРАЗУ (кормит обустройство), бонус «все три» → +монеты и +кристаллы. `DayController` зовёт `BeginDay`/`Report`; авто-клейм «заказа дня» убран. Сейв `dailyTasksDate/Claimed/BonusClaimed`.
6. **Онбординг разбит** (`TutorialController`+`DayController`): старт дня 1 — только базовая петля (убран устаревший шаг «3 ошибки»); в **конце дня 1** — `ShowFirstDayLessons` (знакомство с задачами и обустройством + подарок +150 монет = крючок «на что копить»). tip: `tip_meta`.

**Фаза B (отношения/рецепты/альбом):**
7. **Отношения** (`GameManager`): визит копит `journalRelXp` (парал. journalKeys), уровень 0..5 (60xp/ур), `RelationshipTipBonus` (+2%/ур к чаевым, читает `DayController`).
8. **Рецепты+мастерство** (`Core/RecipeBook.cs`): хорошая подача (`result≥0.6`) двигает счётчик по `CoffeeType`; уровень 0..3 (8 подач/ур) → `MasteryBonus` (+3%/ур к оплате этого напитка). Врезка `DayController.ReportServe`.
9. **Альбом** — раздел хаба: гости X/total + рецепты X/total, награды за полные наборы (`albumSetsClaimed`).

**Фаза C (лайв-опс):**
10. **Событие-турнир** (`Core/EventManager.cs`): недельное (ISO-неделя), очки=звёзды подач, вехи `{25,60,120}`→кристаллы `{3,6,12}`; сброс на новой неделе. Сейв `eventWeek/eventProgress/eventTierClaimed`.
11. **Сезонный пасс «Дневник Миры»** (`Core/SeasonPass.cs`): 30 ур. (35xp/ур), free-трек монеты, premium-трек кристаллы (купить за 150 крист.). Сейв `passXp/passClaimed/passPremium`.
12. **Колесо удачи** (`UI/RewardWheelUI.cs`): ежедневный спин (переменная награда), 2-й спин за rewarded-рекламу. Сейв `wheelLastSpin`.
13. **Хаб прогресса** (`UI/ProgressHubUI.cs`): кнопка «Прогресс» (лев. колонка) + меню разделов (Рецепты/Альбом/Событие/Сезон/Колесо), бейдж доступных наград.

XP событий/сезона идёт со звёзд подач (`DayController`). Левая колонка HUD (сверху вниз): касса → кристаллы → виджет цели → «Задачи дня» → «Прогресс».

> ⚠️ Метрика: новые цели `reno_stage, task_claim, task_allthree, recipe_master, event_progress, pass_level, pass_premium, wheel_spin, album_set`. Всё сейв-совместимо (старые поля не тронуты).

> ⚠️ После Батч 15: **Build Scene Systems + UI** (создаст `RenovationVisualizer` + якоря `RenoAnchors` + виджет цели). Затем **в сцене расставить `RenoAnchor_0..11`** по залу (позиции сохранятся при пересборке) и сохранить сцену. Баланс: обустройство и гейт «10 000 к дню 40» делят монеты — при необходимости подправить.

---

## Батч 16 — полировка потока и UI (Mini UI, последовательность, магазин камерой)

**Единый стиль Mini UI.** Новые окна больше не рисуют плоские прямоугольники: UI/UiSkin.cs
(сцен-синглтон, заполняет билдер) хранит ссылки на спрайты/префабы Mini UI; UI/UiKit.cs —
статические билдеры панелей/кнопок/иконок/шкал из этого скина. На UiKit переведены
Wheel/GemShop/DayChest/RenovationShop/RushHud.

**Последовательные модалки.** UI/UiQueue.cs — очередь блокирующих окон (по одному): сундук дня,
авто-показ выполненной задачи, преображение кофейни. Перед экраном результатов дня GameManager
ждёт WaitWhile(UiQueue.IsBusy) → сначала сундук, потом результат.

**Валюты в едином виде.** Билдер инстанцирует готовые виджеты Mini UI Coin Count/Gem Count
(рамка+иконка+число+плюс); UI/CurrencyWidget.cs обновляет число и вешает магазин на «плюс»
(кристаллы→GemShop, монеты→магазин-обустройство). CoinsUI/CurrencyHudUI сведены к заглушкам
(CoinsUI.JourneyGoal сохранён).

**Магазин-обустройство камерой** (UI/RenovationShopUI.cs + RenovationVisualizer): заменил плашку
«Копим». Справа на HUD кнопка «Магазин» + «!»-значок (когда хватает на следующую покупку). Облёт
камерой точек RenoStages, стрелки листают, будущий предмет мигает, показана цена; покупка ставит
предмет (RenovationManager.Complete) + бонус/сюжет. RenovationGoalUI — заглушка.

**Сундук дня** (UI/DayChestUI.cs): показывается ПЕРВЫМ, спрайт сундука наполняется снизу вверх
(вертикальная маска), затем pop-вскрытие. LootSystem — крупнее и вариативнее, редкий «джекпот».

**Колесо** (UI/RewardWheelUI.cs): реально крутится — вращаем спрайт 5052447 (6 секторов) с ease-out
до сектора-приза (_pointerOffsetDeg для калибровки).

**Точечные фиксы:** топпинг завсегдатая = требуемый топпинг заказа; RushHudUI перенесён в свободный
зазор и прячется на паузе; интро-камера globalSpeedMultiplier=1.35; герой не встаёт на мебель
(рейкаст игнорит RenoStages); сток кристаллов виден (обмен gems→coins + «Ускорить»).
