# caffe-game — Схематика проекта (для аналитика)

> Живой документ. Обновляется куратором после каждой итерации улучшений.
> Игра: **«Междумирье»** — нарративный тайкун-симулятор бариста для **Яндекс Игр** (WebGL, десктоп+мобайл). Unity, плагин YG2.
> Жанр-референс: гибрид «time-management кофейня» + «visual novel». Ядро = приготовить кофе по неявному заказу → раскрыть кусочек сюжета.

---

## 1. Сюжет и структура
- Героиня **Мира** (в enum — Anna) держит кофейню «Междумирье» на границе миров; ищет пропавшего мужа **Кая/Андрея**. Антагонист — **Орден Трёх Кругов** и злодейка **Лира**.
- **40 дней** (`Difficulty.FinalDay = 40`), контент на ~42 дня в `StoryDatabase`. Каждый день = 1+ гость.
- Каждый гость — из своего мира (`CharacterType`, ~30 типов), даёт **реплики приветствия → заказ → (если доволен) сюжетное раскрытие / (если нет) реплики недовольства**.
- Между днями: экран результата дня → опц. **виньетка** (спец-эффект + текст) → **«Сон»** (затемнение + эффект из пула + строка кошмара + **ночной эмбиент** «шёпот призраков») → переход. **Порядок звука/рекламы:** музыка глохнет на ночь, во «Сне» играет ночной эмбиент (`AudioController.PlayNight/StopNight`), он выключается на пробуждении → показывается реклама (`Transition`) → и лишь ПОСЛЕ неё возвращается фоновая музыка нового дня.
- **Цель путешествия:** накопить **10000 монет** (`CoinsUI.JourneyGoal`) к дню 40. Не хватило → `JourneyGate`: начать заново (монеты сохраняются) ИЛИ докупить монеты.
- Финал: Кай спасён, светлая концовка.

## 2. Поток одного дня (`DayController.RunDay`)
Заставка «ДЕНЬ N» → по гостям: `ServeCustomer`:
1. Спавн модели гостя (stickman-префаб), ГГ виден за стойкой.
2. Этап 0: гость идёт к стойке (`ProcessVisitor`).
3. Этап 1: приветственный диалог (`DialogueDisplayer.PlayDialogueLines`). Текст появляется **по словам за ~3 с** (`revealDuration`), 1-й клик — показать всю реплику, 2-й клик — следующая (защита `advanceGuard` от быстрого проскока); сбоку мигает «нажмите для продолжения» (`BlinkText`). Бубнёж SpeechMixer.
4. Полоса удовлетворённости = **запомненное** отношение клиента (память по `CharacterType`, старт 50%).
5. **Готовка** (`CoffeeCraftingSystem`): выбор основы → подтвердить → минигейм машины → топпинги → «Подать».
6. Оценка `EvaluateSatisfaction()` (доля из 4 параметров: основа, температура, объём, топпинг).
7. Кружка передаётся гостю; звёзды 1–3⭐ + празднование.
8. Обновление памяти клиента = среднее(старое, новое).
9. Если клиент недоволен (<0.5) → предложить **Комплимент за 50 монет** (+0.3 настроения).
10. **Оплата** (формула ниже) + списание себестоимости.
11. Реакция гостя (эмоция) + сюжетные/недовольные реплики.
12. Гость уходит. Прогресс сохраняется после КАЖДОГО гостя (`SetCustomerIndex`) — обновление страницы продолжает с места.

День провальный ТОЛЬКО если `_coinsEarnedToday < 0` (в минус) → рестарт дня. Провала «за 3 ошибки» нет.

## 3. Экономика (главный рычаг — деньги)
- **Формула оплаты за напиток** (`DayController` стр. 211-217):
  `payment = round( coinsPerCorrectOrder × payScale(=4) × result × (0.5 + tipMood) × EarlyEase(day) × PriceMultiplier(апгрейд) × combo )`
  - `result` 0..1 — качество напитка.
  - `tipMood = clamp01(память клиента + MoodBonus апгрейда)`.
  - `EarlyEase`: ×1.5 день1 → ×1.0 день6 (мягкий старт).
  - `combo`: серия хороших напитков (≥0.6), +0.15/уровень, макс 5 → до ×1.75.
- **Себестоимость:** основа 8 монет, топпинг 3. `_coinsEarnedToday += payment - drinkCost`.
- **Стартовый капитал:** 100 монет.
- **Апгрейды** (`UpgradeShopUI`, макс ур.3): Beans (+12%/ур к оплате, цена 300×ур), Machine (+допуск 0.04/ур, 250×ур), Loyalty (+чаевые 0.06/ур, 200×ур).
- **Сложность** (`Difficulty`): допуск машины 0.22→0.10 к финалу.

## 4. Монетизация (Яндекс)
- **Interstitial (межстраничная):** на каждом переходе между днями (`Transition`).
- **Rewarded (за награду):**
  - «Подсказка» — точный заказ, кнопка **«Уточнить заказ 📺» доступна ВСЕГДА** (добровольно), пульс-подсветка после 2 провалов подряд (`_adHintButton` → `RewardedAdvShow("hint")`). [итер.1]
  - «Нет денег» панель — реклама за монеты (`AdForCoins`, `_noMoneyPanel`).
  - JourneyGate — докупить монеты.
  - **Экран результата дня** (`DayResultUI`): rewarded «Удвоить выручку дня» (id `day_double`, при earned>0) + «Сохранить серию комбо» (id `combo_save`, при серии ≥3 → перенос на завтра) [итер.2].
- **Daily bonus** (`DailyBonusUI`): вход раз в день, растёт со стриком (base + step×(streak-1), капом `maxReward`).
- **Лидерборд** «coins» по общему балансу.
- Звук и геймплей ставятся на паузу на время рекламы и при потере фокуса окна.

## 5. Загадка заказа (ядро вовлечения)
- Игрок видит НЕ точный заказ, а **расплывчатый намёк** (`VagueHint`): «Хочется <основа>, <тёплого>, <в самый раз>…». Точный — только за рекламу-подсказку.
- Соответствие заказа: `type→основа (index%N)`, `sweet→температура` (0.2/0.45/0.65/0.9), `volume→объём` (0.25/0.55/0.85), `topping`.
- **Топпинги (батч рефакторинга):** enum `Topping` = реальные предметы-модели с полки `ShelfItems` (BellPepper, BundtCake, Cookies, Salami, Salmon, Wasabi, Lollipop, Tomato, Pretzel). Билдер (`MakeToppingItems`/`ToppingByName`) сопоставляет топпинг по **имени дочернего объекта**, а не по индексу → визуальная модель = enum = упоминание в реплике совпадают. Реплики гостей переписаны под полочные топпинги (кекс/леденец/печенье; шутка «без васаби»).
- ⚠️ Примечание: `sweet` маппится на ТЕМПЕРАТУРУ машины (не на сахар) — историческое упрощение.

## 6. Минигейм машины (`MachineMinigame`)
Две вертикальные шкалы по очереди (температура, затем объём): бегунок пинг-понгом (`_speed=0.8`), клик фиксирует уровень. Допуск = `Difficulty.Tolerance(day) + апгрейд`. Попал в оба → «В точку!».

## 7. Карта кода (Assets/Scripts)
- **Core/**: `GameManager`(582, singleton, поток игры/дни/сейв/апгрейды/лидерборд/реклама), `DayController`(281, день+экономика), `GameEnums`(типы, `CoffeeOrder`, `CustomerData`, `GameSaveData`), `Difficulty`, `GameInput`(Locked), `Loc`(RU/EN).
- **Gameplay/**: `CoffeeCraftingSystem`(626, готовка/подсказки/комплимент/себестоимость), `MachineMinigame`(97), `CupController`(203), `CustomerController`(352, модель/эмоции/маршрут), `SatisfactionBar`(120), `IngredientItem`, `HeroIdle`.
- **Story/**: `StoryDatabase`(1745, `DayData/DayCustomerEntry/DialogueLine`, контент дней + пул шуток для повторных гостей) + `Assets/StoryDatabase.asset`.
- **Dialog/**: `DialogueDisplayer`(276, печ.машинка/имя/клик/заставки/сообщения), `DialogueManager`(164), `DialogLine`.
- **UI/**: `CoinsUI`, `DayResultUI`, `HintManager`, `UpgradeShopUI`, `DailyBonusUI`, `JourneyGateUI`, `AdForCoins`, `SettingsUI`, `TutorialController`(292), `UiEffects`(263, комбо/звёзды/монеты/баннеры).
- **Audio/**: `AudioController`(звуки: pour/ding/perfect/coin/star/fail…).
- Прочее: `Stages`(машина этапов 0-7 + камера), `ProcessVisitor`(маршрут гостя), `SpeechPlayer/Fragment`(бубнёж), `FlamesController`, FX/SDK/Effects/CutScene.

## 8. Известные ограничения / зацепки для улучшений
- Нет прямой продажи мягкой валюты пакетами (только rewarded за монеты).
- Один гость на день часто → короткая петля; нет «наплыва»/очереди на скорость.
- Нет ежедневных целей/квестов кроме сюжета; нет коллекций/журнала персонажей.
- `sweet→температура` — неинтуитивно, но менять данные дорого.
- Комбо сбрасывается каждый день; нет «мета-прогрессии» украшения кофейни (визуал апгрейдов).
- ~~Реклама-подсказка только после 2 провалов~~ → [итер.1] теперь добровольна всегда. Всё ещё мало ДРУГИХ rewarded «по желанию» (нет удвоения выручки/сохранения комбо на экране результата).

---
## Журнал итераций
_(куратор дополняет после каждого цикла)_

- **Итерация 0 (базлайн):** схематика составлена. Изменений в код ещё не вносилось.
- **Итерация 1 — rewarded-подсказка стала добровольной ✅** (`CoffeeCraftingSystem.cs`, `DayController.cs`). Кнопка «Уточнить заказ 📺» теперь видна на КАЖДОМ заказе, а не только после 2 провалов. Прежний триггер «2 провала» превращён в пульс-подсветку (`SetAdHintHighlight`). Эффект: rewarded-показы доступны все 40 дней добровольно (успешный игрок в потоке тоже может смотреть), без forced-фрустрации. Точный заказ остаётся «страховкой», неявный намёк по-прежнему угадываем без рекламы.
  - _Обновление раздела 4/8 схематики:_ rewarded «подсказка» теперь доступна всегда (не только после провалов) → зацепка «мало rewarded по желанию» частично закрыта.
  - Ещё не сделано из итерации 1: постоянный трекер «монеты/10000» с прогнозом темпа; экран «Отношения»/журнал персонажей.
- **Итерация 2 — «Сохранить комбо» на экране результата ✅** (`DayResultUI.cs`, `DayController.cs`, `GameManager.cs`). Обнаружено: rewarded «Удвоить выручку дня» (`_btnDouble`, id `day_double`) УЖЕ существовал. Добавлена новая rewarded-кнопка «Сохранить серию ×N 📺» (id `combo_save`), доступна при серии ≥3; переносит комбо на следующий день (`GameManager.CarriedCombo`, потребляется в начале дня). Эффект: вторая добровольная rewarded-точка в тёплый момент + мост потока между днями (мотив вернуться завтра). Баланс: перенос одноразовый (сгорает после старта дня), только при достойной серии.
  - Прогноз-трекер «монеты/10000» и наплыв/очередь гостей — предложены аналитиком, пока НЕ сделаны (кандидаты на следующие итерации).
- **Итерация 3 — трекер «Путь к 10 000» с прогнозом темпа ✅** (`DayResultUI.cs`). На экране результата: полоса `total/10000` + цветная строка-прогноз. Логика: needPerDay=(10000−total)/(40−day) vs actualPerDay=total/day → зелёный «Опережаешь»/жёлтый «Впритык»/красный «Отставание ~N/день — удвой выручку 📺». Дни <7 — нейтральное «Разгоняемся…» (искажение EarlyEase). Все UI-поля опциональны (нет ссылки → трекер молчит). Эффект: снимает шок финального гейта, направляет к rewarded «Удвоить» в нужный момент. Зацепка «нет трекера цели» закрыта.
  - Осталось из предложений аналитика: наплыв/очередь гостей (часы пик); журнал персонажей + мета-полки + endless-режим после финала.
- **Итерация 4 — журнал гостей «Завсегдатаи» ✅ (код)** (`SavesYG.Game.cs`, `GameManager.cs`, `DayController.cs`, новые `Core/CharacterNames.cs`, `UI/JournalCard.cs`, `UI/GuestJournalUI.cs`). Делает видимой память отношений: карточка на тип гостя (имя, статус, визиты, лучшие ⭐, симпатия %). Хранение: новые параллельные списки `journalKeys/Visits/BestStars`. Запись визита в `DayController` после подачи. Пассивный бонус «завсегдатая»: симпатия ≥90% → +5% к чаевым. Прогресс «Знакомств X/N» — коллекционная мета, переживающая сюжет.
  - ⚠️ **Требует ручной сборки в Unity-редакторе:** панель журнала + ScrollView(Content) + шаблон-карточка с `JournalCard` (назначить TMP/Image поля) + кнопки «Журнал»/«Закрыть», ссылки в `GuestJournalUI`. Кнопку открытия удобно повесить на экран результата дня. Без арт-ассетов (только TMP/Image/цвета).
  - Осталось: наплыв/«часы пик»; endless-режим после дня 40.
- **Итерация 5 — скилл-бонус «✨ Идеально» в машине ✅** (`CoffeeCraftingSystem.cs`, `DayController.cs`). Попадание точно в центр обеих осей (±0.03, у́же любого допуска) → «✨ Идеально» + ×1.1 к оплате (`PrecisionBonus`). Превращает бинарный клик в градиент мастерства → глубже «поток», честный скилл. Чистая логика, без сборки UI/контента. Баланс: бонус скромный (×1.1) и трудный (±0.03), экономику не инфлирует.
  - **Решение куратора:** рекомендованный аналитиком «Наплыв/очередь гостей» отложен как крупная рискованная инициатива — перестраивает последовательное ядро `ServeCustomer`, требует одновременных моделей/маршрутов и сборки в сцене; внедрять вслепую опасно для рабочей игры. Кандидат на отдельную сессию с запуском Unity. Также в очереди: daily-квесты «Заказ дня», endless после дня 40, реворк семантики осей машины (крепость/температура).
- **Итерация 6 — «Заказ дня» (daily-квест) ✅** (новый `Gameplay/DailyChallenge.cs`, интеграция в `DayController.cs`). Ежедневный детерминированный квест (seed = номер дня → одинаков у всех игроков = топливо для лидерборда). 5 типов: заработать N монет / N× «Идеально» / 3⭐ / продать 2 топпинга / напиток ≥80%. Прогресс копится по дню (`ReportDrink`), награда (60–150+, растёт с днём) начисляется в конце дня и входит в выручку. Анонс через `ShowMessage`, HUD-строка опциональна. Эффект: ежедневная цель (D1/D7 retention), апселл топпингов, повод для лидерборда. Баланс: награда ≤~12% выручки, цели выполнимы при 1 госте/день.
  - ⚠️ Опционально в редакторе: повесить `DailyChallenge` на объект, задать HUD-TMP, привязать ссылку `_dailyChallenge` в `DayController`. Без ссылки — просто не активен (безопасно).
  - Осталось из предложений: апселл «любимый топпинг» (журнал↔экономика); endless-режим после дня 40.
- **Итерация 7 — апселл «любимый топпинг» ✅** (`CharacterNames.cs`, `CoffeeCraftingSystem.cs`, `DayController.cs`). У каждого типа гостя детерминированный любимый топпинг (`CharacterNames.FavoriteTopping`, хэш→1 из 5). При симпатии ≥60% в фазе топпингов — намёк «Гость любит: X ☕». Положил → ×1.08 к оплате и +0.03 симпатии; проигнорировал явную просьбу → −0.02 симпатии. Связывает журнал с экономикой (второй контур ценности лояльности), поднимает средний чек у ядра. Баланс: множитель скромный, штраф мягкий, для новых гостей (симпатия <60%) нейтрально.
  - Осталось: endless-режим после дня 40; онбординг-подсказки новых механик; напоминание о риске стрика.
- **Итерация 8 — онбординг «Первый контакт» ✅** (`SavesYG.Game.cs`, `GameManager.cs`, `DayController.cs`). Одноразовые контекстные подсказки при первом срабатывании механик: `tip_perfect` (Идеально ×1.1), `tip_regular` (завсегдатай/журнал), `tip_favorite` (апселл топпинга), `tip_quest` (Заказ дня синхронен у всех). Флаги в сейве (`shownTips` + `GameManager.MarkTipShown`), показ через `ShowMessage`, максимум 1 подсказка за гостя. Чинит ранний отвал (D1/D3), «зажигает» механики удержания/монетизации у новичка.
  - Осталось: endless-режим после дня 40; «Особый гость»/босс-день (пики вовлечения); напоминание о риске стрика; визуализация апгрейдов.
- **Итерация 9 — дожим экрана результата + предупреждение о стрике ✅** (`DayResultUI.cs`). (1) Loss-aversion: при стрике входа ≥3 строка «Стрик N дней 🔥 загляни завтра — иначе сгорит!» (`_streakWarningText`, опц.). (2) Пульс-дожим кнопки «Удвоить выручку» (мягкая пульсация, `unscaledDeltaTime`, стоп при клике/закрытии). Кнопка уже показывает конкретную сумму `+N`. Эффект: рост CTR rewarded «Удвоить» + D1/D2 retention через напоминание о стрике. Без новой сборки UI (правка существующего экрана).
  - Осталось: endless-режим после дня 40; «Особый гость»/босс-день.
- **Итерация 10 — «Особый гость / Знак дня» ✅** (`DayController.cs`, `CoffeeCraftingSystem.cs`, `GameManager.cs`). Каждый 8-й день (8/16/24/32/40) — особый: ставка ×1.3 (`_specialDayMult`), допуск у́же на −0.02 (`SetExtraTolerance`), анонс в начале дня + тизер «кто-то особенный» накануне в «Сне». Создаёт ритмичные пики вовлечения (якоря-ожидания D8→D40), снижает провал середины. `DayController.IsSpecialDay(day)` — общий хелпер. Баланс: ставку держим ×1.3 (не ×1.4), чтобы стэк с «Удвоить» не ломал темп цели; провал по-прежнему не наказывает сверх обычного.

## ИТОГ 10 ИТЕРАЦИЙ — что внедрено
1. rewarded-подсказка «Уточнить заказ» доступна всегда (добровольно).
2. rewarded «Сохранить комбо» (перенос серии на завтра).
3. Трекер «Путь к 10 000» с цветным прогнозом темпа.
4. Журнал гостей «Завсегдатаи» (видимая память отношений + бонус завсегдатая).
5. Скилл-бонус «✨ Идеально» (±0.03 → ×1.1).
6. «Заказ дня» — ежедневный детерминированный квест (синхронен у всех).
7. Апселл «любимый топпинг» (журнал↔экономика, ×1.08).
8. Онбординг «Первый контакт» (одноразовые контекстные подсказки).
9. Дожим экрана результата (пульс «Удвоить») + предупреждение о сгорании стрика.
10. «Особый гость / Знак дня» (пики вовлечения на днях ×8).

## Батч рефакторинга (полировка UX/атмосферы) — что внедрено
Отдельный проход после 10 итераций LTV (по запросу владельца):
1. **Показ диалога по словам** (`DialogueDisplayer`): вместо посимвольной «печатной машинки» реплика раскрывается по словам за ~3 с (`revealDuration`). 1-й клик — показать всю реплику, 2-й — следующая; `advanceGuard` (~0.35 с) не даёт быстрым кликом проскочить диалог целиком.
2. **Мигающий хинт «нажмите для продолжения»** (`BlinkText`) вместо статичного «далее ▸». Локализован (ru/en).
3. **Вариативность «Заказа дня» до 10 типов** (`DailyChallenge`): + ServeDrinks, StarCollector, HighSatisfaction, TwoStarsPlus, BigEarnings (seed = день, синхронно у всех).
4. **Реструктуризация топпингов** (`GameEnums`, `CoffeeCraftingSystem`, билдер): enum = предметы полки `ShelfItems`, сопоставление по имени объекта; реплики/подсказки приведены в соответствие (см. раздел 5).
5. **Порядок перехода: сначала реклама, потом музыка** (`GameManager`): `ResumeMusic` перенесён на после `Transition` (см. раздел 1).
6. **Обучение (день 1): скрыты все кнопки HUD, кроме настроек** (`TutorialController.HideHudButtons/RestoreHudButtons` — прячет `BtnJournal`, `BtnHint`, возвращает после обучения).
7. **Ночной звук** «шёпот призраков» (`Assets/Audio/night_ambience.mp3` → `AudioController._nightAmbience`, отдельный зацикленный источник): вкл. с началом «Сна», выкл. до рекламы.
8. **Чистка неподдерживаемых символов** в подписях/сообщениях: шрифт Nunito не содержит ✓ ☕ ✨ 🔥 📺 ⭐ ★ ☆ — все удалены из UI-строк кода (звёзды в журнале → «N / 3»), чтобы не было «квадратиков».
9. **Цветные иконки HUD** из `Mini UI/Icons` (Settings/Book/Blue Energy) вместо линейных из `UI Icons`; иконки не тонируются (`Color.white`).
10. **Схематика обновлена** (этот файл + `ARCHITECTURE_MAP.md`).

> ⚠️ После правок кода нажать **Tools → CoffeGame → Build Scene Systems + UI** в Unity: пересоберёт HUD (цветные иконки, мигающий хинт), привяжет `_nightAmbience` и топпинги по именам полки. Импорт `night_ambience.mp3` произойдёт при открытии редактора.

## Главные возможности на будущее (не сделаны)
1. **Endless-режим после дня 40** — единственный источник retention/дохода у «доигравших»; отдельный лидерборд.
2. **Наплыв/очередь гостей (часы пик)** — скилл-механика темпа; отложена как крупная рискованная (перестройка ядра `ServeCustomer` + сборка в сцене).
3. **Визуализация апгрейдов и мастерства** (числа «до/после», ширина зоны) — конверсия «понятной ценности» в rewarded/LTV.

## Сборка сцены — через кнопку-билдер (автоматизировано)
Вся сцена (системы + Canvas + UI + привязка `[SerializeField]`) собирается редакторским скриптом
`Assets/Editor/CoffeGameSceneSetup.cs` → меню **Tools → CoffeGame → Build Scene Systems + UI**
(идемпотентно: удаляет прошлые `--- GAME SYSTEMS ---` и `GameCanvas` и строит заново).

**Батч 6 полностью встроен в билдер** — по нажатию кнопки создаются и привязываются:
- `DailyChallenge` (объект в GAME SYSTEMS) + HUD-строка «Заказ дня» (слева под кассой) → `DayController._dailyChallenge`.
- Экран результата: трекер «Путь к 10 000» (прогресс + полоса + прогноз), предупреждение о стрике, кнопка «Сохранить комбо» — все поля `DayResultUI` привязаны.
- Экран журнала «Завсегдатаи»: панель + ScrollRect(Viewport/Content) + шаблон `JournalCard` + кнопки «Журнал»/«Закрыть», привязка `GuestJournalUI`.
- Хелперы билдера: `HorizontalFill`, `MakeScrollView`, `MakeJournalCard`.

Порядок после правок кода: в Unity нажать кнопку билдера → проверить в Play → сохранить сцену (Ctrl+S) → коммит.
Чисто-логические фичи (Идеально, апселл, особый день, онбординг) работают без сборки.

## Батч 7 — исправления и полировка (сцена сна + топпинги + UI)
1. **Диалог** снова печатается ПОСИМВОЛЬНО за `revealDuration` (~3с), не рывками по словам (`DialogueDisplayer.ShowLineRoutine`, кадровое накопление). 1-й клик — вся реплика, 2-й — дальше.
2. **Комбо-надпись** авто-гаснет через ~2с после показа (`UiEffects.ComboPop`), а не висит до сброса серии.
3. **Топпинги переписаны** (главный баг):
   - Единый источник привязки имя-модели→enum: `ToppingUtil.FromObjectName` (в `GameEnums.cs`); используется и билдером, и `IngredientItem.Awake` в РАНТАЙМЕ — кликнутая модель всегда даёт свой топпинг (салями = Salami, не рыба, не случайно).
   - Один слот: клик выбирает/заменяет, повторный клик снимает; списание денег и падение в кружку — только по кнопке **Подтвердить** (`CoffeeCraftingSystem.SelectTopping`/`OnConfirmTopping`).
   - Заказ НАЗЫВАЕТ топпинг явно (`VagueHint`); случайные заказы берут топпинг только из `ToppingUtil.ShelfToppings` (что реально есть на полке); `SetTargetOrder` снимает недоступный топпинг.
4. **Сцена сна (`Sleepy scene`)** — полная смена сцены между днями. `GameManager.RunGameDays`: итоги+магазин в MainScene → `LoadScene("Sleepy scene")` → `SleepSceneController` (ночной звук + титр «СОН» + эффект + текст сна, строит свой UI в коде) → реклама → `LoadScene("MainScene")`. Состояние — в `YG2.saves` (currentDay/`sleepFromDay`); статик-флаги в GameManager не дают повторить обучение/бонус/GameReady при пере-входе. Запасной путь (старый оверлей в MainScene), если сцена не в Build Settings.
5. **UI-полировка**: цель дня крупнее (HUD 20→30); убрана дублирующая верхне-правая кнопка-подсказка (остаётся пульс-иконка); апгрейды показываются в % эффекта (`+36% → +48%`) вместо «Уровень N/5»; кнопка магазина на итогах пульсирует + бейдж числа доступных апгрейдов + автооткрытие; **кнопка «Убрать рекламу»** (`AdDisableButton`, YG2 Payments, товар `no_ads`, флаг `adsDisabled` глушит интерстишелы); **бейдж журнала** (`JournalBadge`) с числом новых гостей + пульс, сброс при открытии; упрощён жаргон (стрик/апселл → человеческие формулировки).

> ⚠️ Требуется в Unity: **Tools → CoffeGame → Build Scene Systems + UI** (пересобрать MainScene: топпинги, крупная цель, бейдж журнала, кнопка «Убрать рекламу», без боковой подсказки) И **Tools → CoffeGame → Build Sleep Scene** (собрать `Sleepy scene`: `SleepController` + ночной звук + шрифт, добавить обе сцены в Build Settings). Для покупки «Убрать рекламу» — завести товар `no_ads` в консоли Яндекс Игр.
